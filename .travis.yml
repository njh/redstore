sudo: required

language: c

install:
  - sudo apt-get update -qq
  - sudo apt-get install -y build-essential libtool pkg-config pandoc
  - sudo apt-get install -y librdf-dev librdf-storage-sqlite

script:
  - ./autogen.sh
  - make
  - make test
